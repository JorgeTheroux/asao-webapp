package dgtic.core.repository;

import dgtic.core.entity.Plagas;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlagasRepository extends JpaRepository<Plagas, Integer> {
}
